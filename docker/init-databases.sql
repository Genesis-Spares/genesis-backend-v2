-- Each service owns its own database (see *_DATABASE_URL in docker-compose.yml).
CREATE DATABASE genesis_auth;
CREATE DATABASE genesis_customer;
CREATE DATABASE genesis_notification;
CREATE DATABASE genesis_catalog;
CREATE DATABASE genesis_order;
